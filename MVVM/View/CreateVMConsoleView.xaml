<UserControl x:Class="Manager.MVVM.View.CreateVMConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="568" d:DesignWidth="744">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="Создание VM - Консоль" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="24" Margin="10,10,0,0"/>

        <TextBox x:Name="ConsoleOutput" 
                 Grid.Row="1" 
                 Foreground="White" 
                 Background="Black" 
                 FontFamily="Consolas" 
                 FontSize="14" 
                 IsReadOnly="True" 
                 Text="{Binding ConsoleText, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" 
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"/>
    </Grid>
</UserControl>
